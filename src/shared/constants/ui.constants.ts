/**
 * Minimum time (in ms) to display skeleton loaders.
 * Ensures skeleton is visible for at least this duration to avoid flicker.
 */
export const MINIMUM_SKELETON_TIME = 500

/**
 * Maximum time (in ms) to wait for loading before considering it a timeout.
 * After this duration, loading is considered failed.
 */
export const LOADING_TIMEOUT = 60000
